#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
int sum (int init,int endpointt);

int main(int argc, char *argv[]) {
	// »¼¥[½m²ß 

	printf("summary=%d\n",sum(5,10));
	return 0;
}
